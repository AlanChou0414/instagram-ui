import { SafeAreaView } from 'react-native';
import { SearchBar } from './_layout';

const SearchScreen = () => {
  return (
    <SafeAreaView>
      <SearchBar />
    </SafeAreaView>
  );
};

export default SearchScreen;
