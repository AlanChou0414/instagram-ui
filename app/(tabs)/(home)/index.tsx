import { SafeAreaView } from 'react-native';
import { PostList } from './_layout';

const HomeScreen = () => {
  return (
    <SafeAreaView>
      <PostList />
    </SafeAreaView>
  );
};

export default HomeScreen;
